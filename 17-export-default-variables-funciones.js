//exportar una sola variable o funcion, no pueden haber 2 export default
//en el mismo archivo.
const tarea = 'Pasear al perro'
export default tarea

// const crearTarea = (tarea, urgencia) => {
//     return `la ${tarea} es ${urgencia}`
// }
// export default crearTarea